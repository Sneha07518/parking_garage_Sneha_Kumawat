async function api(path, options={}, auth=true){options.headers={'Content-Type':'application/json',...(options.headers||{})};if(auth){const token=localStorage.getItem('parksmart_token');if(token)options.headers.Authorization=`Bearer ${token}`;else location='/login.html'}const response=await fetch(path,options);if(response.status===401||response.status===403){localStorage.removeItem('parksmart_token');if(auth)location='/login.html';throw new Error('Please log in again')}const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error?.message||'Request failed');return data}